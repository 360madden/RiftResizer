# Profiles Module

Responsibilities:
- save and load named layout profiles
- validate profile compatibility with current monitor topology
- handle versioning/migration of saved profile schema if needed

Profiles should store intent clearly and fail safely when the current hardware setup no longer matches.
