# Source

Implementation code lives here.

## Suggested Subfolders
- `core/` — shared types, config, and logging
- `windowing/` — window enumeration and resize/move logic
- `profiles/` — saved layouts and restore logic
- `ui/` or `cli/` — presentation layer

Do not let this grow into a dumping ground. Keep boundaries strict.
