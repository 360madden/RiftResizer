# RiftResizer

Initial repository scaffold for the RiftResizer project.

## Purpose
A workspace for tooling related to resizing, layout, and window management for RIFT MMO workflows.

## Initial Structure
- `src/` — source code
- `docs/` — notes and documentation
- `assets/` — static assets

This is a starting point only and can be refactored once the actual implementation scope is locked.
